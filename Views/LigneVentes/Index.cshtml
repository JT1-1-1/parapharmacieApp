@model IEnumerable<ParapharmacieApp.Models.LigneVente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrixUnitaire)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProduitNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdVenteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrixUnitaire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProduitNavigation.IdProduit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVenteNavigation.IdVente)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLigneVente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLigneVente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLigneVente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
